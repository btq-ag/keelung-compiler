# modified from https://github.com/simonmichael/hledger/blob/master/.github/workflows/linux.yml

name: Profiling

on:
  push:
    branches:
      - profiling

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    - name: 📥 Checkout repository
      uses: actions/checkout@v2

    - name: Setup upterm session
      uses: lhotari/action-upterm@v1

    # Make sure the @v0.5.4 matches the current version of the
    # action 
    - uses: webfactory/ssh-agent@v0.5.4
      with:
        ssh-private-key: |
          ${{ secrets.KEELUNG_CI }}
          ${{ secrets.KEELUNG_EXAMPLES_CI }}
          ${{ secrets.KEELUNG_PROFILING }}

    # things to be cached/restored:

    - name: 💾 Cache stack global package db
      id:   stack-global
      uses: actions/cache@v2
      with:
        path: ~/.stack
        key: ${{ runner.os }}-stack-global-profiling-${{ matrix.plan.ghc }}-${{ hashFiles('**.yaml') }}
        restore-keys: |
             ${{ runner.os }}-stack-global-profiling-${{ matrix.plan.ghc }}
    - name: 💾 Cache stack-installed programs in ~/.local/bin
      id:   stack-programs
      uses: actions/cache@v2
      with:
        path: ~/.local/bin
        key: ${{ runner.os }}-stack-programs-profiling-${{ matrix.plan.ghc }}-${{ hashFiles('**.yaml') }}
        restore-keys: |
             ${{ runner.os }}-stack-programs-profiling-${{ matrix.plan.ghc }}
    - name: 💾 Cache .stack-work
      uses: actions/cache@v2
      with:
        path: .stack-work
        key: ${{ runner.os }}-stack-work-profiling-${{ matrix.plan.ghc }}-${{ hashFiles('**.yaml') }}
        restore-keys: |
             ${{ runner.os }}-stack-work-profiling-${{ matrix.plan.ghc }}
    - name: 💾 Cache keelung-compiler/.stack-work
      uses: actions/cache@v2
      with:
        path: keelung-compiler/.stack-work
        key: ${{ runner.os }}-keelung-compiler-stack-work-profiling-${{ matrix.plan.ghc }}-${{ hashFiles('keelung-compiler/package.yaml') }}
        restore-keys: |
             ${{ runner.os }}-keelung-compiler-stack-work-profiling-${{ matrix.plan.ghc }}
    - name: 💾 Cache keelung-examples/.stack-work
      uses: actions/cache@v2
      with:
        path: ./keelung-examples/.stack-work
        key: ${{ runner.os }}-keelung-examples-stack-work-profiling-${{ matrix.plan.ghc }}-${{ hashFiles('./keelung-examples/package.yaml') }}
        restore-keys: |
             ${{ runner.os }}-keelung-examples-stack-work-profiling-${{ matrix.plan.ghc }}
    # actions:

    - name: ⏬ Install stack
      run: |
        mkdir -p ~/.local/bin
        export PATH=~/.local/bin:$PATH
        # curl -sL https://get.haskellstack.org/stable/linux-x86_64.tar.gz | tar xz --wildcards --strip-components=1 -C ~/.local/bin '*/stack'; chmod a+x ~/.local/bin/stack
        if [[ ! -x ~/.local/bin/stack ]]; then curl -sL https://get.haskellstack.org/stable/linux-x86_64.tar.gz | tar xz --wildcards --strip-components=1 -C ~/.local/bin '*/stack'; chmod a+x ~/.local/bin/stack; fi
        stack --version
    - name: ⏬ Install GHC
      run: |
        df -h
        stack setup --install-ghc
        df -h
    - name: 🔨 Build and install the compiler with profiling enabled
      run: |
        stack install --profile
    - name: 📥 Checkout examples
      uses: actions/checkout@v3
      with:
        repository: 'btq-ag/keelung-examples'
        path: './keelung-examples'
        ref: 'profiling'
        ssh-key: ${{ secrets.KEELUNG_EXAMPLES_CI }}
    - name: 🔨 Build the examples
      run: |
        cd ./keelung-examples
        stack build --flag keelung-examples:profile
    - name: 🩺 Run profilings
      run: |
        stack exec profile > /dev/null
        cp hash.prof $GITHUB_WORKSPACE
        cp MerkleProof.prof $GITHUB_WORKSPACE

    - name: 📥 Checkout profiling repo
      uses: actions/checkout@v3
      with:
        repository: 'btq-ag/Keelung-Profiling-Results'
        path: './results'
        ref: 'main'
        ssh-key: ${{ secrets.KEELUNG_PROFILING }}

    - name: Test ls
      run: |
        ls "$GITHUB_WORKSPACE/keelung-examples"

    - name: Remove old profiling files
      run: |
        find "$GITHUB_WORKSPACE/results" -mindepth 1 -delete

    - name: 🔥 Install ghc-prof-flamegraph
      run: |
        stack install ghc-prof-flamegraph

    - name: 🍳 Prepare .prof files and flamegraphs
      run: |
        cd $GITHUB_WORKSPACE
        ls keelung-examples
        find keelung-examples -name "*.prof" -exec mv '{}' results \;
        find $GITHUB_WORKSPACE -name "*.prof"
        find $GITHUB_WORKSPACE -name "*.prof" -exec mv '{}' results \;
        cd results
        find . -name "*.prof" -exec ghc-prof-flamegraph --alloc $f -o $f-alloc.svg \;
        find . -name "*.prof" -exec ghc-prof-flamegraph --time $f -o $f-time.svg \;
        mkdir flamegraphs
        find . -name "*.svg" -exec mv '{}' flamegraphs \;
        git add -A
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git commit -m 'profile with compiler@$GITHUB_SHA'
    - name: Wait a while
      run: |
        sleep 30m

    - name: 🚢 Ship profiling results
      uses: cpina/github-action-push-to-another-repository@main
      env:
        SSH_DEPLOY_KEY: ${{ secrets.KEELUNG }}
      with:
        source-directory: 'results'
        destination-github-username: 'btq-ag'
        destination-repository-name: 'Keelung-Profiling-Results'
        user-email: action@github.com
        target-branch: main
